{
  "name": "testr",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "mocha **/*.test.js index.test.js --ui exports --recursive --reporter spec --check-leaks",
    "test-watch": "mocha **/*.test.js index.test.js --ui exports --recursive --reporter spec --check-leaks --watch"
  },
  "devDependencies": {
    "chai": "4.1.2",
    "mocha": "5.0.5"
  }
}
